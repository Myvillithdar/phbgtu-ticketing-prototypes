@model IEnumerable<phbgtu_ticketing_prototypes.Models.CustomFormFieldDataOption>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.DataOptionValue)
                </th>
            <th>
                @Html.DisplayNameFor(model => model.CustomFormFieldQuestion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DataOptionValue)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CustomFormFieldQuestion.CustomFormFieldQuestionID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CustomFormFieldDataOptionID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CustomFormFieldDataOptionID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CustomFormFieldDataOptionID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
