@model IEnumerable<phbgtu_ticketing_prototypes.Models.CustomFormFieldDatatype>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.DatatypeName)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DatatypeName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CustomFormFieldDatatypeID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CustomFormFieldDatatypeID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CustomFormFieldDatatypeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
